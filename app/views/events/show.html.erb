<%= render "shared/header" %>
<%=@event.title%>
<%=@event.content%>
    <% if user_signed_in? && current_user.id == @event.user_id %>
    <%=link_to "削除", event_path(@event.id), method: :delete, class: "event-destroy"%>
    <%=link_to "編集", edit_event_path(@event.id),method: :get, class: "event-edit"%>
    <% end %>